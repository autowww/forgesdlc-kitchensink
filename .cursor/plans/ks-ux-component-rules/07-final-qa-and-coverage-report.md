# KS UX component rules — phase 07 (final QA and coverage report)

## Status

**PASS**

All required builds and automated checks completed with exit code **0**. An extra verification pass ran **`check-visual-catalog.mjs` with `--strict-contract-governance`**; it also exited **0** with no warnings.

---

## Files changed (this phase)

| Artifact | Role |
|----------|------|
| `.cursor/plans/ks-ux-component-rules/07-final-qa-and-coverage-report.md` | This evidence report (new). |
| `docs/design/catalog/visual-registry-coverage.md` | Regenerated by `check-visual-catalog.mjs` (**Generated:** `2026-05-19T06:01:41.487Z` at time of last checker run). |

Broader initiative edits (phases 01–06) span design contracts under `docs/design/catalog/**`, UX governance docs under `docs/design/ux-audit/`, `visual-registry.yaml` / generated JSON/MD inventory, `.cursor/rules/ks-ux-component-rules-governance.mdc`, and tooling under `tools/website-ux-auditor/` and `tools/design-catalog/`. See phases **01–06** plan files for scoped file lists.

---

## Deterministic rules added/changed (summary)

Cross-phase, recurring UX signals are encoded in:

| Area | Mechanism |
|------|-----------|
| Live DOM proxies | `tools/website-ux-auditor/lib/dom-metrics.js` (context complexity, hero/visual rhythm, progressive-disclosure proxies, CTA spread, etc.). |
| Deterministic checks | `checks/readability-structure.js`, `first-screen-density.js`, `product-visual.js`, `technical-depth.js`, `cta-trust-ecosystem.js`, `visual-catalog-awareness.js`. |
| Catalog / contracts | `tools/design-catalog/lib/contract-specificity.mjs` (thin generic **Expected look**, duplicate slabs, layout **States**, registry basename↔hash alignment); invoked from `check-visual-catalog.mjs`. |
| Remediation metadata | Example finding tags: **`DET.VISUAL.RHYTHM`** (`first-screen-density`), **`DET.CATALOG.HASH_REGISTRY_UNKNOWN`** (`visual-catalog-awareness`); defect plans resolve hashes via **`visual-registry.generated.json`** only (no YAML parser in the auditor runtime path). |

**`analyze-website-ux.mjs`** and **`score-website-ux.mjs`** remain separate entrypoints (shared **`lib/`** only).

---

## AI-enabled principles added/changed (summary)

| Artifact | Change |
|----------|--------|
| `docs/design/ux-audit/ai-enabled-design-principles.md` | Canonical **`principleId`** values; required judgment metadata on AI findings (`deterministicCoverage`, `candidateDeterministicRule`, `hashesOrContractsAffected`, etc.). |
| `tools/website-ux-auditor/lib/ai-audit-batches.js` | **`AI_REVIEW_CONTRACT`**, batch embedding, **`normalizeAiFinding`** / aggregator markdown that surfaces principle + candidate deterministic promotion. |
| `cursor-agent-run-ux-audit.sh` | Prompt maps findings to **`principleId`** and steers **`candidateDeterministicRule`** for repeatable patterns. |

AI remains **advisory** for premium feel, narrative coherence, and ambiguous visuals; deterministic passes gate Major+ remediation first.

---

## Catalog contract coverage

Source: regenerated **`docs/design/catalog/visual-registry-coverage.md`** (checker OK, **91** registry entries).

| Metric | Value |
|--------|-------|
| Registry entries | 91 (**active:** 91) |
| **`contract_status: own`** | 54 |
| **`contract_status: family-covered`** | 37 (listed in coverage doc § **Intentional family-covered rows**) |
| **`emits_html: true`** | 74 |
| **`emits_html: false`** | 17 |
| Inventory items (`visual-inventory.generated.json`) | 369 |

**Registry ↔ inventory:** coverage report states all non-family registry rows matched at least one inventory item; **“Needs registry attention: (none)”**.

---

## Generic contract text remaining (if any)

- Phase **05** removed the legacy duplicated **“Calm Forge enterprise atmosphere…”** slab and added slug-keyed **Expected look** / **Responsive** blurbs for stateful **`own`** contracts; family roll-ups (**`Ksc`**, **`Kpr`**, **`Ksj`**, **`Ksv`**, **`Msm`**, **`Rpf`**) carry **concrete roles** plus linkage to **`docs/design/forge-enterprise-ai-website-standard.md`**, not undifferentiated boilerplate.
- **`check-visual-catalog.mjs --strict-contract-governance`** on this repo: **zero warnings** (verification run during phase 07).
- **`contract-template.md`** documents governance headings for new contracts; intentional family-covered rows remain **justified** in the coverage appendix.

---

## Screenshots / live showcase status

| Signal | Result |
|--------|--------|
| **`python3 generator/build-showcase.py`** | **PASS** — **22** showcase pages + layout previews written under `showcase/`. |
| **Screenshot status (registry)** | **captured:** 48 · **not-applicable:** 41 · **blocked:** 2 (per `visual-registry-coverage.md`). |
| **Live React bundle** | Showcase build copied **`showcase-react-app/dist/`** into showcase output (build log confirms). |

---

## Tests / checks run

Commands executed for phase 07 (repo root unless noted):

```bash
python3 generator/build-showcase.py
```

```bash
cd tools/website-ux-auditor && npm test && cd ../..
```

```bash
cd tools/design-catalog && npm install && cd ../..
```

```bash
node tools/design-catalog/check-visual-catalog.mjs --repo . --registry docs/design/catalog/visual-registry.yaml --showcase showcase
```

**Additional QA (not in the minimal prompt, recommended):**

```bash
node tools/design-catalog/check-visual-catalog.mjs --repo . --registry docs/design/catalog/visual-registry.yaml --showcase showcase --strict-contract-governance
```

**Results:** showcase build **OK**; **`npm test`** in **`tools/website-ux-auditor`** → **103/103** pass; **`tools/design-catalog`** dependencies **up to date**; visual catalog checker → **`check-visual-catalog OK (91 entries)`** (both default and strict runs).

---

## Known limitations

1. **Blocked screenshots (2):** registry marks two hashes as **`blocked`**; contracts still govern behavior—capture is deferred or tooling-limited.
2. **Museum Vite chunks:** **21** content-addressed paths under `museum/studio/assets/*` stay inventory-only; policy keeps **`Msm`** family-level registration until artifact names stabilize (documented in coverage report § **Uncovered or deferred museum inventory paths**).
3. **Judgment-only UX:** premium “feel”, cross-section narrative, and ambiguous diagram intent remain **AI-first** even when geometry/alt heuristics pass (see phase **03** / **`ai-enabled-design-principles.md`**).

---

## Follow-up tasks

1. When new showcase slugs ship, extend **`tools/design-catalog/lib/contract-element-blurbs.mjs`** and re-run **`apply-element-specific-blurbs.mjs`** if **Expected look** drifts toward generic copy (phase **05** playbook).
2. Revisit **blocked** screenshot rows when capture pipeline allows; keep **`screenshot_status`** honest in **`visual-registry.yaml`**.
3. If museum bundle filenames stabilize, consider child registry rows or documented exceptions beyond **`Msm`** family coverage.
4. Continue promoting repeatable AI findings via **`candidateDeterministicRule`** into **`DET.*`** checks or catalog validators.

---

## Fleet-specific profile

**No Fleet-specific profile was created.** Fleet appears only as a **generic** docs-first / mechanism-led regression example in governance rules and copy guidance, consistent with **`.cursor/rules/ks-ux-component-rules-governance.mdc`**.

---

*Completed: 2026-05-19 (phase 07 final QA).*
