# Default Theme Contract Overlays

This directory is intentionally empty for the initial default theme. The base
visual contracts under `docs/design/catalog/` remain authoritative.

Add overlay Markdown files here only when a theme needs element-specific
expectations that cannot live in the shared base contract.
