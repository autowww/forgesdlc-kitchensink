# Tower Cubes Loader (`Twr`)

**Hash:** `Twr` · **Slug:** `tower-cubes-loader` · **Showcase:** `#sec-tower-cubes-loader`

Emitter: `components/spatial_wave2.py` or `components/spatial.py` · CSS: `css/ks-spatial-wave2.css`

## Purpose

Tower cubes loader

## Expected behavior

Governed spatial primitive with hash marker, reduced-motion fallback, and preserve-3d support gate.

## Oracle scenarios

See [`../oracles/Twr.json`](../oracles/Twr.json).

## Accessibility

- Root emits `hash` and `data-ks-hash="Twr"`.
- Motion respects `prefers-reduced-motion: reduce`.
