# Sphere Cube Intersection (`Xsc`)

**Hash:** `Xsc` · **Slug:** `sphere-cube-intersection` · **Showcase:** `#sec-sphere-cube-intersection`

Emitter: `components/spatial_wave2.py` or `components/spatial.py` · CSS: `css/ks-spatial-wave2.css`

## Purpose

Sphere cube intersection

## Expected behavior

Governed spatial primitive with hash marker, reduced-motion fallback, and preserve-3d support gate.

## Oracle scenarios

See [`../oracles/Xsc.json`](../oracles/Xsc.json).

## Accessibility

- Root emits `hash` and `data-ks-hash="Xsc"`.
- Motion respects `prefers-reduced-motion: reduce`.
