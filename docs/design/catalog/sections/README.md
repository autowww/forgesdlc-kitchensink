# Sections

Section-level contracts (composable regions inside pages) can live here as **`{HASH}-{slug}.md`** rows when they are first-class catalog entries.

Many showcase sections remain **family-covered** under page or component contracts in [visual-registry.yaml](../visual-registry.yaml). Add a section contract when a region has a stable reuse boundary and its own governance needs.
