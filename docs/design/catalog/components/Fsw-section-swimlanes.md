# Fsw — Section swimlanes dock

**Hash:** `Fsw` · **Type:** component · **Family:** section-swimlanes · **Status:** active

## Expected look

Horizontal swimlane dock with section chips; 3D lift on active lane.

## Deterministic checks

- Root `div.fs-section-swimlanes` emits `hash="Fsw"` and `data-ks-hash="Fsw"`.

## Showcase

`showcase/section-swimlanes.html`
